// CodeOwner: @js_owner

// This is a single-line comment

/*
 * This is a multi-line
 * block comment in JavaScript.
 */

const x = 42;  // Inline comment

function greet(name) {
    // Another single-line comment
    return `Hello, ${name}!`;
}

const result = greet("world");  /* Inline block comment */
console.log(result);
